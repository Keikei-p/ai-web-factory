# local: current Windows/SQLite mode
VITE_APP_MODE=local

# cloud: set VITE_APP_MODE=cloud and fill the two values below.
# The publishable key is intended for browser use. Never put a service_role key here.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
